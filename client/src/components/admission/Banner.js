import React from 'react';

const Banner = (props) => {
    return (
        <section className="admission-banner">
            <div className="banner-overlay">hello</div>
        </section>
    );
};

export default Banner;